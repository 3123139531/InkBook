server:
  port: 80
spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
#      url: jdbc:mysql://localhost:3306/jdbc-test?serverTimezone=UTC
      url: jdbc:mysql://localhost:3306/mydbtest?serverTimezone=UTC
      username: root
#      password: mysql182469
      password: 1234
  main:
    banner-mode: off

mybatis-plus:
  global-config:
    db-config:
      id-type: auto
  configuration:
    #    日志信息（便于调试）
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
